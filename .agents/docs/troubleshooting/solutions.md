# Solutions

**Hydration**: Wrap in `useEffect` + `mounted` state check
**Image**: Add Cloudinary to `next.config.js` `remotePatterns`
**Translation**: Add key to `locales/en/common.json` + `locales/es/common.json`, use `t('key')`
**New page**: Create `pages/X/index.js` → add `getStaticProps` → update `utils/constants/index.js` nav
